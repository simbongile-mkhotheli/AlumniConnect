/**
 * Sponsors Services
 * Centralized export for all sponsors-related services
 */

export { SponsorsService } from './sponsorsService';
export { SPONSORS_ENDPOINTS } from './endpoints';

// Re-export service as default for convenience
export { SponsorsService as default } from './sponsorsService';
